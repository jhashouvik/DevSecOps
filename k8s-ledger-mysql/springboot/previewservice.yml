apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: wezvaspringbootapp
  name: ledgernew-svc
  namespace: demo
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name: http
    nodePort: 31100
  selector:
    app.kubernetes.io/name: wezvaspringbootapp
  type: NodePort
