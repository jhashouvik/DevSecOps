apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: wezva-auth
  namespace: demo
spec:
  method: kubernetes
  mount: kubernetes

  kubernetes:
    role: wezvatech-admin
    serviceAccount: wezvatech-admin

  vaultConnectionRef: vault-connection
